import 'package:flutter/material.dart';

class UnitTrip extends StatelessWidget {
  const UnitTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
